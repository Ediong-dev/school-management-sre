# TODO: Migrate to SealedSecrets or ExternalSecrets in Lab 5.
# This is committed to Git only because it's a lab secret with a known value.
# Never commit real secrets. Ever.

apiVersion: v1
kind: Secret
metadata:
  name: auth-secret
  namespace: school
type: Opaque
stringData:
  jwt-secret: "dev-secret-change-in-prod"