apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: dashboard
  namespace: school
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: dashboard
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
